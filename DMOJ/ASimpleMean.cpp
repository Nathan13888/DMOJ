// #include <bits/stdc++.h>
#include <iostream>
#include <math.h>
using namespace std;

//
int main()
{
    // ios_base::sync_with_stdio(false);
    // cin.tie(NULL);

    int a, b, c;
    cin >> a >> b >> c;

    cout << (a + b + c) / 3;

    return 0;
}