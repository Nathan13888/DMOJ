#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int N, H, chrl, enme;
    cin >> N >> H;
    vector<pair<char, int>> moves;
    chrl = enme = H;

    return 0;
}