#include "bits/stdc++.h"
using namespace std;

int calc (int quant,string form) {
    
}

// INFO: given up in favour of better alternatives online
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    string left[2], right[2];

    cout << "Enter the space-separated values on the left" << "\n";
    cin >> left[0];
    cin >> left[1];

    cout << "Enter the space-separated values on the right" << "\n";

    cin >> right[0];
    cin >> right[1];
    
    return 0;
}